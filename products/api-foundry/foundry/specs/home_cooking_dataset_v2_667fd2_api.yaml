openapi: 3.0.3
info:
  title: My Dataset
  version: 1.0.0
  description: 'Auto-generated API schema from Home_Cooking_Dataset_v2_667fd2.xlsx.
    CRITICAL IMPLEMENTATION INSTRUCTION: Do NOT generate a mock response or HTTP request.
    Inside the tool handler, you MUST `import pandas as pd`, use `pd.read_csv` or
    `pd.read_excel` to load exactly ''C:\Users\cheta\Documents\coding\mcp_server\data\Home_Cooking_Dataset_v2_667fd2.xlsx'',
    filter the dataframe using the tool arguments, and return the data as a JSON string.'
paths:
  /search:
    post:
      summary: Search the My Dataset dataset
      operationId: searchDataset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties: &id001
                '#':
                  type: integer
                  description: 'Data column: #'
                Dish Name:
                  type: string
                  description: 'Data column: Dish Name'
                Cuisine / Region:
                  type: string
                  description: 'Data column: Cuisine / Region'
                Cook Time:
                  type: string
                  description: 'Data column: Cook Time'
                Difficulty:
                  type: string
                  description: 'Data column: Difficulty'
                Key Ingredients:
                  type: string
                  description: 'Data column: Key Ingredients'
                "Healthy \u2605":
                  type: string
                  description: "Data column: Healthy \u2605"
                Cost $$$:
                  type: string
                  description: 'Data column: Cost $$$'
                Calories:
                  type: string
                  description: 'Data column: Calories'
      responses:
        '200':
          description: List of matching records
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties: *id001
